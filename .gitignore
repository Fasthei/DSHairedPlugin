# ── 插件运行数据：含 Jina API Key 与真实目标资产，绝不提交 ──
.redteam-assets.json
*.redteam-assets.json
.redteam-asset-graph.rules.md

# 攻击矩阵按工作区落盘的扫描结果，同样是真实目标数据
.redteam-attack-matrix.json
*.redteam-attack-matrix.json

# 记忆插件的设置与日志：里面有 Milvus Token、向量/重排模型 API Key、S3 的 AK/SK，绝不提交
.redteam-memory.json
*.redteam-memory.json

# 动态装载器（开发形态）写出的装载诊断，每次装载都会重写
.rtmatrix-dev.json
.rtmemory-dev.json

# ── 导出产物（本地生成） ──
redteam-assets-*.json
redteam-assets-*.csv
redteam-assets-*.md

# ── 通用 ──
node_modules/
*.log
.DS_Store
.env

# ── 打包产物（本地生成，不该提交）──
*.tgz
.npm-cache/

# ── 临时校验目录 ──
.packtest/
.check/

# ── GitHub Packages 发布副本（由脚本生成）──
build/

# 红队报告插件的落盘文件（含报告全文；默认落在插件自己的工作区，但用户可以改成仓库路径）
.redteam-report.json
*.redteam-report.json
.rtreport-dev.json
